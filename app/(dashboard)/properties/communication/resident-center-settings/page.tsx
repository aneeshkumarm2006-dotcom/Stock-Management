// Phase 11 ComingSoon placeholder. Resident Center settings are deferred per
// PROPERTY_TODO.md Phase 11.
import { ComingSoon } from "@/components/pm/ComingSoon";

export const metadata = {
  title: "Resident Center settings — Property Management",
};

export default function ResidentCenterSettingsPage() {
  return <ComingSoon title="Resident Center settings" />;
}
